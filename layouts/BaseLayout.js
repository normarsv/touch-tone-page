import React from "react";

const BaseLayout = ({}) => {
  return (
    <div>
      <label>test</label>
    </div>
  );
};

BaseLayout.propTypes = {
  someData: PropTypes.string,
};

export default BaseLayout;
